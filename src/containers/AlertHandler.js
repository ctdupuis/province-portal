import React, { Component } from 'react';
import Alert from '../components/static/Alert';

export default class AlertHandler extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
